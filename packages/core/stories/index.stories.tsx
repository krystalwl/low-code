import { Editor as MaterialEditor } from '@/editor';
import { Example } from '@/example';

export const BasicEditor = () => <MaterialEditor />;
export const BasicExample = () => <Example />;

export default {
  title: 'Button 按钮',
};
